name: Update odds
on:
  schedule:
    - cron: "0 */8 * * *"   # every 8 hours (two API calls per run; free tier is 500/month, shared with super_six)
  workflow_dispatch:
permissions:
  contents: write
jobs:
  odds:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node scripts/fetch-odds.mjs
        env:
          ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
      - run: |
          git config user.name "odds-bot"
          git config user.email "actions@users.noreply.github.com"
          git add odds.json
          git diff --cached --quiet || { git commit -m "odds update"; git push; }
